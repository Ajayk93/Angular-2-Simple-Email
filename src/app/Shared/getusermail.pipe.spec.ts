import { GetusermailPipe } from './getusermail.pipe';

describe('GetusermailPipe', () => {
  it('create an instance', () => {
    const pipe = new GetusermailPipe();
    expect(pipe).toBeTruthy();
  });
});
